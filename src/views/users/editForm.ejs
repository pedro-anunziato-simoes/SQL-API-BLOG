<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionar Usuario</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Editar Usuario</h1>
        <form action="/users/edit/<%= user.id%>" method="POST">
            <div class="mb-3">
                <label for="id" class="h5">ID do usuário: <%= user.id%></label>
                <br>
                <label for="name" class="form-label">Nome</label>
                <input type="text" id="name" name="name" placeholder="Digite seu nome" value="<%=user.name %>" class="form-control" required>
                <label for="name" class="form-label" >E-mail</label>
                <input type="text" id="email" name="email" placeholder="Digite o email" value="<%=user.email %>" class="form-control" required>
                <label for="senha" class="form-label">Senha</label>
                <input type="text" name="senha" id="senha" placeholder="Digite a senha" value="<%=user.senha %>" class="form-control" required>
                <label for="name" class="form-label">Papel:</label>
                <select name="papel" id="papel" class="form-control" required>
                    <option value="Usuario">Usuario</option>
                    <option value="Adminitrador" selected>Adminitrador</option>
                </select>
                <div class=" mt-3">
                    <% if(user.fl_ativo){ %>
                    <input type="checkbox" name="ativo" id="ativo" checked class="form-check-input" >
                    <label for="ativo" class="form-check-label">Ativo</label>
                    <%} else{ %>
                        <input type="checkbox" name="ativo" id="ativo" class="form-check-input" >
                    <label for="ativo" class="form-check-label">Ativo</label>
                    <%}%>
                 </div> 
            </div>
            <button type="submit" class="btn btn-primary">Salvar</button>
        </form>
        <a href="/users" class="btn btn-secondary mt-3">Ver Usuarios</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    </script>
</body>
</html>